<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:LibrarySystem.UI.Controls"
             x:Class="LibrarySystem.UI.Views.FavoritesPage"
             Title="Favorilerim"
             Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#2F1B68" Offset="0.0" />
            <GradientStop Color="#B63B6E" Offset="0.55" />
            <GradientStop Color="#F29B3A" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">

        <Grid Grid.Row="0"
              Background="{StaticResource HeaderGradient}"
              Padding="14,48,14,20"
              RowDefinitions="Auto,Auto"
              RowSpacing="10">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Button Text="←"
                        FontSize="20"
                        TextColor="White"
                        BackgroundColor="Transparent"
                        Padding="4"
                        Clicked="Back_Clicked" />
                <BoxView Grid.Column="2" WidthRequest="44" Color="Transparent" />
            </Grid>

            <VerticalStackLayout Grid.Row="1"
                                 Spacing="6"
                                 HorizontalOptions="Center">
                <Label Text="Favorilerim"
                       TextColor="White"
                       FontSize="18"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center" />
                <Label Text="Kaydettiğin kitaplar"
                       TextColor="#E8E0FF"
                       FontSize="13"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Grid>

        <CollectionView Grid.Row="1"
                        x:Name="FavoritesList"
                        Margin="12,8,12,0"
                        SelectionMode="None"
                        BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundColorLight}, Dark={StaticResource PageBackgroundColorDark}}"
                        EmptyView="Henüz favori kitap eklemedin. Ana sayfadan kitapları kaydedebilirsin.">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,12"
                            Padding="14,12"
                            Style="{StaticResource ThemedCard}"
                            StrokeShape="RoundRectangle 16">
                        <Border.Shadow>
                            <Shadow Brush="#00000018" Offset="0,4" Radius="10" Opacity="0.3" />
                        </Border.Shadow>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14" RowDefinitions="Auto,Auto">
                            <Border Grid.RowSpan="2"
                                    WidthRequest="52"
                                    HeightRequest="72"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource AccentSoftPurpleColorLight}, Dark={StaticResource AccentSoftPurpleColorDark}}"
                                    VerticalOptions="Center"
                                    Clip="RoundRectangle 10">
                                <controls:BookCoverImage BookId="{Binding BookId}"
                                                         PlaceholderFontSize="28" />
                            </Border>
                            <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                <Label Text="{Binding BookTitle}"
                                       FontAttributes="Bold"
                                       TextColor="{AppThemeBinding Light={StaticResource TextPrimaryColorLight}, Dark={StaticResource TextPrimaryColorDark}}"
                                       FontSize="16"
                                       LineBreakMode="TailTruncation" />
                                <Label Text="{Binding BookAuthor}"
                                       TextColor="{AppThemeBinding Light={StaticResource TextSecondaryColorLight}, Dark={StaticResource TextSecondaryColorDark}}"
                                       FontSize="13"
                                       LineBreakMode="TailTruncation" />
                                <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                                    <Label Text="★" TextColor="#FFB800" FontSize="14" />
                                    <Label Text="{Binding RatingDisplay}"
                                           TextColor="{AppThemeBinding Light={StaticResource TextSecondaryColorLight}, Dark={StaticResource TextSecondaryColorDark}}"
                                           FontSize="13"
                                           FontAttributes="Bold" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            <Button Grid.Column="2"
                                    Grid.RowSpan="2"
                                    Text="🔖"
                                    FontSize="18"
                                    TextColor="White"
                                    BackgroundColor="#5E4BB6"
                                    CornerRadius="12"
                                    WidthRequest="48"
                                    HeightRequest="48"
                                    VerticalOptions="Center"
                                    CommandParameter="{Binding .}"
                                    Clicked="RemoveFav_Clicked" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Border Grid.Row="2"
                Style="{StaticResource ThemedTabBar}"
                StrokeShape="RoundRectangle 24"
                Padding="14,10"
                Margin="14,8,14,14">
            <Grid ColumnDefinitions="*,*,*,*,*" ColumnSpacing="6">
                <Button x:Name="TabHomeBtn" Grid.Column="0" Text="🏠&#10;Ana Sayfa" Style="{StaticResource ThemedTabInactive}" Clicked="TabHome_Clicked" />
                <Button x:Name="TabCategoriesBtn" Grid.Column="1" Text="🗂️&#10;Kategoriler" Style="{StaticResource ThemedTabInactive}" Clicked="TabCategories_Clicked" />
                <Button x:Name="TabFavoritesBtn" Grid.Column="2" Text="🔖&#10;Favorilerim" Style="{StaticResource ThemedTabInactive}" />
                <Button x:Name="TabNotificationsBtn" Grid.Column="3" Text="🔔&#10;Bildirimler" Style="{StaticResource ThemedTabInactive}" Clicked="TabNotifications_Clicked" />
                <Button x:Name="TabSettingsBtn" Grid.Column="4" Text="⚙️&#10;Ayarlar" Style="{StaticResource ThemedTabInactive}" Clicked="TabSettings_Clicked" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>
